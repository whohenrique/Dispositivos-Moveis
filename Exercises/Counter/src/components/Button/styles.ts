import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  containerButton: {
    justifyContent: "center",
    alignItems: "center",
    width: 150,
    height: 80,
    borderRadius: 8,
  },
  buttonText: {
    color: "white",
    fontSize: 20,
    fontWeight: "bold",
  },
});
