import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 100,
  },
  containerCounter: {
    fontSize: 50,
  },
  containerButtons: {
    justifyContent: "center",
    alignItems: "center",
    gap: 30,
    width: 200,
  },
});
